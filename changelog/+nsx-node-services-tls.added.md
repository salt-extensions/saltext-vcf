Extended ``vcf_nsx_node_services`` with ``cipher_suites`` / ``protocols`` support on the HTTP service: new client ``http_tls_set`` and state ``tls_configured(name, protocols=None, cipher_suites=None)`` that read-merge-PUT so DoS-mitigation fields stay intact. Satisfies the ISA "Encryption Requirements / Enable TLS 1.2" control (STIG 912 series) requiring TLS 1.2+ and disabling unapproved cipher suites per the ISA Cryptographic Requirements document.
