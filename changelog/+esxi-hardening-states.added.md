Named 912 Controls VM/host hardening states so compliance SLS reads as control names rather than opaque ``advanced_settings=`` dicts: ``vcf_vim_vm_hardening.console_options_locked`` / ``.hgfs_disabled`` / ``.log_rotation_configured`` wrap the VM extraConfig surface, and ``vcf_esxi_advanced.tps_disabled`` wraps the ``Mem.ShareForceSalting`` / ``Mem.ShareScanGHz`` host pair. Ergonomic rewrapping only -- the underlying controls already worked via raw dicts.
