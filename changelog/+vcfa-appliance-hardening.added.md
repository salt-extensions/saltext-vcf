Add ``vcfa_appliance`` client, ``vcf_vcfa_appliance`` execution module, and ``vcf_vcfa_appliance`` state with ``tls_configured`` / ``service_disabled`` / ``ssh_configured`` verbs to enforce the three 912 Controls VCF Automation appliance-hardening items (TLS 1.2/1.3 minimum, unused-port disable, and ROOT/ADMIN SSH access). Uses the VCFA appliance management-plane REST surface on port 5480; on lab profiles where those endpoints are gated to appliance shell only the states will surface the appliance's ``com.vmware.vcfms.api.*`` error and hardening should be applied out-of-band (deferred — see the client docstring for details).
