Add VCFA session-policy, identity-provider, and vRA→vIDM peer-cert clients (``vcfa_session_policy``, ``vcfa_identity_provider``, ``vcfa_vidm_peer``) with exec-module wrappers and idempotent states so 912-controls hardening runs can enforce the max-auth-failures / inactive-timeout defaults, register AD/LDAP IdPs for portal SSO, and rotate the vIDM peer TLS trust anchor via ``/csp/gateway/am/api`` and ``/provisioning/mgmt/identity-vidm-peer``.
