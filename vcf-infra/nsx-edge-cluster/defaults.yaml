# Exec-only bridge via `module.run` — no dedicated state exists yet for
# vcf_nsx_edge_cluster. NOTE: not idempotent — create's `body` has no
# caller-supplied id (server-generated), so there's no clean existence
# check before creating.
edge_cluster:
  body:
    display_name: edge-cluster-01
    member_node_type: EDGE_NODE
    members:
      - transport_node_id: edge-node-01
      - transport_node_id: edge-node-02
