{% import_yaml "log-forwarder/defaults.yaml" as defaults %}

{# Note: pillar.get's merge=True replaces lists wholesale, it does not merge
   list items one by one. To point at a different syslog target, redefine
   the full "syslog.servers" list in the pillar/data override rather than
   editing a single field of it. #}
{% set cfg = salt['pillar.get']('log-forwarder', default=defaults, merge=True) %}
